.text
    lui  x1, 0x10010        
    addi x1, x1, 0x100      
    
    addi x2, x0, 42         # Data to store
    sw   x2, 0(x1)          # Write 42 to the Magic Address
    lw   x3, 0(x1)          # Read 42 back from the Magic Address
    addi x4, x3, 10         # *** LOAD-USE STALL TEST *** (x4 should become 52)
    
    addi x17, x0, 10        # RARS Halt Code
    ecall